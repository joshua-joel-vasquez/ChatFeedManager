# Python dependencies for the full bot stack.
#
# Install (after you open a terminal in this `bot/` folder):
#   pip install -r requirements.txt

python-dotenv>=1.0.0
requests>=2.31.0
websockets>=11.0.0
psutil>=5.9.0
spotipy>=2.23.0
